import type { QAProps } from '../../types';
import type { ListItemCommonProps, ListItemId, ListItemListContextProps, ListItemSize, ListOnItemClick, RenderItemProps, UseListResult } from '../types';
type ItemRendererProps<T> = QAProps & {
    size?: ListItemSize;
    /**
     * Affects the view of the selected items
     */
    multiple?: boolean;
    id: ListItemId;
    mapItemDataToProps(data: T): ListItemCommonProps;
    onItemClick?: ListOnItemClick;
    list: UseListResult<T>;
};
/**
 * Map list state and parsed list state to item render props
 */
export declare const getItemRenderState: <T>({ qa, list, onItemClick, mapItemDataToProps, size, multiple, id, }: ItemRendererProps<T>) => {
    data: T;
    props: RenderItemProps;
    context: ListItemListContextProps;
};
export {};
