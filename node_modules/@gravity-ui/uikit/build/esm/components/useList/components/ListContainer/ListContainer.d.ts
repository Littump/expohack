import React from 'react';
import type { ListItemId, UseListResult } from '../../types';
import type { ListContainerViewProps } from '../ListContainerView/ListContainerView';
export type ListContainerProps<T> = Omit<ListContainerViewProps, 'children'> & {
    list: UseListResult<T>;
    containerRef?: React.RefObject<HTMLDivElement>;
    renderItem(id: ListItemId, index: number, 
    /**
     * Ability to transfer props from an overridden container render
     */
    renderContainerProps?: Object): React.JSX.Element;
};
export declare function ListContainer<T>({ containerRef, renderItem, list, ...props }: ListContainerProps<T>): React.JSX.Element;
